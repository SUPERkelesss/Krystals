material {
    name : KrystalsAtomOpaque,
    shadingModel : lit,
    blending : opaque,
    requires : [ tangents ],
    variables : [ customNormal ],
    parameters : [
        { type : float4, name : baseColor },
        { type : float3, name : backgroundColor },
        { type : float2, name : depthRange },
        { type : float, name : depthCueEnabled }
    ]
}
fragment {
    void material(inout MaterialInputs material) {
        prepareMaterial(material);
        vec4 color = materialParams.baseColor;
        // v0.8.29: atom = clear-coat plastic ball (Filament recommended PBR config).
        // Base layer is a matte plastic dielectric; a smooth clear coat on top gives
        // the glossy plastic-ball look. Lighting (ambient 60% / sun 40%) is provided
        // by the scene's IndirectLight + directional LightManager light.
        float viewDepth = (getViewFromWorldMatrix() * vec4(getWorldPosition(), 1.0)).z;
        float divisor = max(materialParams.depthRange.x - materialParams.depthRange.y, 0.0001);
        float fog = materialParams.depthCueEnabled * clamp((materialParams.depthRange.x - viewDepth) / divisor, 0.0, 1.0);
        color.rgb = mix(color.rgb, materialParams.backgroundColor, fog);
        material.baseColor = color;
        material.metallic = 0.0;
        // v0.8.33: no clear coat — plain dielectric base with diffuse + basic
        // reflectance (Fresnel) only. Lighting comes from the scene's 60% ambient
        // IndirectLight + 40% sun directional LightManager light.
        material.roughness = 0.32;
        material.reflectance = 0.56;
    }
}
vertex {
    void materialVertex(inout MaterialVertexInputs material) {
        vec4 q = mesh_tangents;
        vec3 n = vec3(
            2.0 * (q.x * q.z + q.w * q.y),
            2.0 * (q.y * q.z - q.w * q.x),
            1.0 - 2.0 * (q.x * q.x + q.y * q.y)
        );
        material.customNormal = vec4(normalize(getWorldFromModelNormalMatrix() * n), 0.0);
    }
}
