.claude/
.todos/
.gradle/
.idea/
local.properties
CLAUDE.md

# Release signing secrets — never commit.
keystore.properties
keystores.properties
*.keystore
**/build/
*.iml
captures/
.externalNativeBuild/
.cxx/
.tooling/
app/release/

# Activation code secrets — never commit (paid module).
activation-codes.txt
activation-secrets.gradle.kts
