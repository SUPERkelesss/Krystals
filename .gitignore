.claude/
/rebuild/
/worktrees/
.todos/
.gradle/
.gradle-test-root/
.kotlin/
.idea/
local.properties
CLAUDE.md
tmp/

# Release signing secrets — never commit.
keystore.properties
*.keystore
app/*.keystore
**/build/
*.iml
captures/
.externalNativeBuild/
.cxx/
.tooling/
app/release/
# Backup files — never commit.
*.bak

# Activation code secrets — never commit (paid module).
activation-codes.txt
activation-secrets.gradle.kts

# Hermes agent plans/session artifacts
.hermes/
.worktrees/
